const AboutHeader = () => {
  return (
    <div
      className="relative bg-content2 shadow-sm border-b-content1 border-b-8 w-screen h-20 flex items-center justify-center bg-fixed bg-center bg-cover"
/*       style={{ backgroundImage: `url(${backgroundServices})` }} */
    >
      <div className="absolute inset-0 opacity-50"></div>
    </div>
  )
}

export default AboutHeader